'use strict';

import $ from 'jquery';

if ( $('[data-page="videos"]') ) {
  // $( '[data-toggle="collapse"]' ).on( 'click tap', function ( event ) {
  //   event.preventDefault();
  //   let target = $( event.currentTarget ).prop( 'href' );
  //   $( target ).toggle();
  // });
}
